# Sea Sentinel — Requirements for Render Cloud Deployment (CPU-only)
# Uses CPU-only PyTorch to reduce build size for Render free tier.

# Core Frameworks & Computer Vision (CPU-only PyTorch)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision
ultralytics>=8.0.0
opencv-python-headless>=4.8.0
albumentations>=1.3.0
Pillow>=9.5.0

# Scientific & Data Analytics Stack
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.2.0

# Geospatial & GIS Projection
pyproj>=3.5.0

# API & Server Stack
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6
pydantic>=2.0.0
PyYAML>=6.0

# Visualization & Reporting
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
